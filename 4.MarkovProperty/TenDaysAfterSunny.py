
initial_state = [1.0,0.0]

transition_matrix = [
     [0.2,0.8],
     [0.4,0.6]
]
current_state = initial_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

####
# Lines 17-24 were copied and pasted 8 times,
####

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)

current_state = new_state
new_state = [0,0]

for ii in range(len(current_state)):
    for jj in range(len(transition_matrix)):
        new_state[ii] += round(current_state[jj]*transition_matrix[jj][ii],3)

print(new_state)